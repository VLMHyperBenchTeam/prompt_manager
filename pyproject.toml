[project]
name = "prompt_manager"
version = "0.1.0"
description = "Prompt management and resolution for VLMHyperBench"
dependencies = [
    "pydantic>=2.0.0",
    "jinja2>=3.0.0",
]

[build-system]
requires = ["uv-build"]
build-backend = "uv.build"